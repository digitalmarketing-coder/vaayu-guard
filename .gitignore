# Node / Next.js
node_modules/
.next/
out/
dashboard/.env.local
dashboard/.env*.local

# .NET
**/bin/
**/obj/
agent/dist/

# Distribution archives made for copying the agent to another PC
*.rar
*.zip

# Published agent exe staged for auto-update — too large for git (100MB+),
# rebuilt from agent/src on demand via agent/install/stage-update.ps1.
dashboard/public/agent/*.exe

# Local agent runtime data (never commit device tokens)
**/device.token
**/queue.db
**/consent_ack.json

# OS / editor
.DS_Store
Thumbs.db
.vscode/
